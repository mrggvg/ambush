meta {
  name: Create Exit Node Token
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/users/{{userId}}/tokens
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "label": "exit-node-2",
    "expires_at": null
  }
}
